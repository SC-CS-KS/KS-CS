2. 重要范式



范式总结：
最传统 1个 命令式
最基本 2个 命令式和声明式
最核心 3个 命令式、函数式和逻辑式
最主要 4个 命令式、函数式、逻辑式、对象式
最重要 5个 命令式、函数式、逻辑式、对象式、并发式

过程式：以过程为模块的君主体系，模块之间互相授命和听命。
函数式：以函数为模块的数学体系，模块之间互相替换和合成。
逻辑式：以断言为模块的逻辑体系，模块之间互相归纳和演绎。
对象式：以对象为模块的民主体系，模块之间互相交流和服务。
并发式：以进程为模块的生成体系，模块之间互相竞争和合作。